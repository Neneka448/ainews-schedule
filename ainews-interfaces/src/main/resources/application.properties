# AI News Runner - Task Runner Module
spring.application.name=ainews-runr

# Import shared defaults from config-common
spring.config.import=classpath:application-common.yml

# Module-specific logging overrides
logging.level.com.mortis=INFO
logging.level.org.apache.kafka=WARN
